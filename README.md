# SwiftCart - E-Commerce Website

একটি আধুনিক, রেসপন্সিভ ই-কমার্স ওয়েবসাইট যা Tailwind CSS, DaisyUI এবং Fake Store API ব্যবহার করে তৈরি।

## 🚀 Features

- ✅ রেসপন্সিভ ডিজাইন (মোবাইল ফ্রেন্ডলি)
- ✅ ডাইনামিক প্রোডাক্ট লোডিং
- ✅ ক্যাটাগরি ফিল্টারিং
- ✅ প্রোডাক্ট ডিটেইলস মডাল
- ✅ কার্ট ফাংশনালিটি
- ✅ নিউজলেটার সাবস্ক্রিপশন

## 📁 Project Structure

```
shopingSite/
├── index.html          # হোম পেজ
├── products.html       # প্রোডাক্টস পেজ
├── mian.js            # মেইন জাভাস্ক্রিপ্ট ফাইল
├── tailwind.config.js  # Tailwind কনফিগারেশন
├── README.md          # ডকুমেন্টেশন
└── Assets/
    └── banner-image.png  # ব্যানার ইমেজ
```

## 🛍️ API Endpoints Used

- **All Products:** `https://fakestoreapi.com/products`
- **All Categories:** `https://fakestoreapi.com/products/categories`
- **Products by Category:** `https://fakestoreapi.com/products/category/${category}`
- **Single Product:** `https://fakestoreapi.com/products/${id}`

---

## ❓ প্রশ্ন ও উত্তর

### ১) null এবং undefined এর মধ্যে পার্থক্য কি?

**null** এবং **undefined** দুটোই জাভাস্ক্রিপ্টে "কোনো মান নেই" বোঝাতে ব্যবহৃত হয়, কিন্তু এদের মধ্যে গুরুত্বপূর্ণ পার্থক্য আছে:

**undefined:**
- যখন কোনো ভেরিয়েবল ডিক্লেয়ার করা হয় কিন্তু কোনো মান অ্যাসাইন করা হয় না, তখন সেটি স্বয়ংক্রিয়ভাবে `undefined` হয়ে যায়।
- এটি জাভাস্ক্রিপ্ট ইঞ্জিন নিজে থেকে সেট করে।
- যখন কোনো ফাংশন কিছু রিটার্ন করে না, তখনও `undefined` রিটার্ন হয়।
- যখন অবজেক্টে কোনো প্রপার্টি নেই কিন্তু অ্যাক্সেস করার চেষ্টা করা হয়।

```javascript
let name;
console.log(name); // undefined

function test() {}
console.log(test()); // undefined
```

**null:**
- এটি একটি ইচ্ছাকৃতভাবে অ্যাসাইন করা মান যা "কোনো মান নেই" বা "খালি" বোঝায়।
- প্রোগ্রামার নিজে থেকে সেট করে যে এই ভেরিয়েবলে কোনো মান নেই।
- এটি একটি অবজেক্ট টাইপ (`typeof null` দিলে "object" আসে)।

```javascript
let user = null; // ইচ্ছাকৃতভাবে খালি রাখা হয়েছে
console.log(user); // null
```

**মূল পার্থক্য:**
| বিষয় | undefined | null |
|-------|-----------|------|
| অর্থ | মান এখনো অ্যাসাইন হয়নি | ইচ্ছাকৃতভাবে খালি |
| কে সেট করে | জাভাস্ক্রিপ্ট ইঞ্জিন | প্রোগ্রামার |
| typeof | "undefined" | "object" |

---

### ২) JavaScript এ map() ফাংশনের ব্যবহার কি? এটি forEach() থেকে কিভাবে আলাদা?

**map() ফাংশন:**

`map()` হলো একটি অ্যারে মেথড যা একটি অ্যারের প্রতিটি এলিমেন্টের উপর একটি ফাংশন প্রয়োগ করে এবং **নতুন একটি অ্যারে রিটার্ন করে**।

```javascript
const numbers = [1, 2, 3, 4, 5];
const doubled = numbers.map(num => num * 2);
console.log(doubled); // [2, 4, 6, 8, 10]
console.log(numbers); // [1, 2, 3, 4, 5] (অরিজিনাল অ্যারে অপরিবর্তিত)
```

**forEach() ফাংশন:**

`forEach()` ও প্রতিটি এলিমেন্টের উপর ফাংশন প্রয়োগ করে, কিন্তু **কিছুই রিটার্ন করে না** (undefined রিটার্ন করে)।

```javascript
const numbers = [1, 2, 3, 4, 5];
const result = numbers.forEach(num => console.log(num * 2));
// 2, 4, 6, 8, 10 প্রিন্ট হবে
console.log(result); // undefined
```

**প্রধান পার্থক্য:**

| বিষয় | map() | forEach() |
|-------|-------|-----------|
| রিটার্ন | নতুন অ্যারে রিটার্ন করে | undefined রিটার্ন করে |
| চেইনিং | অন্য মেথডের সাথে চেইন করা যায় | চেইন করা যায় না |
| ব্যবহার | ডেটা ট্রান্সফর্ম করতে | সাইড ইফেক্ট (যেমন console.log) এর জন্য |
| পারফরম্যান্স | সামান্য বেশি মেমরি লাগে | কম মেমরি লাগে |

```javascript
// map() দিয়ে চেইনিং
const result = [1, 2, 3]
    .map(x => x * 2)
    .filter(x => x > 2)
    .reduce((a, b) => a + b, 0);
// result = 10

// forEach() দিয়ে এটা সম্ভব না
 

### ৩) == এবং === এর মধ্যে পার্থক্য কি?

**== (Loose Equality / Abstract Equality):**
- দুটি মানের তুলনা করার সময় **টাইপ কনভার্সন** করে।
- শুধু মান সমান কিনা চেক করে, টাইপ চেক করে না।

```javascript
console.log(5 == "5");     // true (স্ট্রিং "5" কে নম্বর 5 এ কনভার্ট করে)
console.log(0 == false);   // true (false কে 0 এ কনভার্ট করে)
console.log(null == undefined); // true
console.log("" == 0);      // true
```

**=== (Strict Equality):**
- **টাইপ কনভার্সন করে না**।
- মান এবং টাইপ দুটোই সমান হতে হবে।

```javascript
console.log(5 === "5");    // false (টাইপ আলাদা: number vs string)
console.log(0 === false);  // false (টাইপ আলাদা: number vs boolean)
console.log(null === undefined); // false
console.log(5 === 5);      // true (মান ও টাইপ দুটোই সমান)
```

**কোনটি ব্যবহার করা উচিত?**

বেশিরভাগ ক্ষেত্রে `===` ব্যবহার করা উচিত কারণ:
- এটি আরো নির্ভুল এবং প্রত্যাশিত ফলাফল দেয়
- বাগ এড়াতে সাহায্য করে
- কোড পড়া সহজ হয়

```javascript
// ভালো প্র্যাক্টিস
if (userInput === "yes") {
    // কাজ করো
}

// এড়িয়ে চলা উচিত
if (userInput == "yes") {
    // অপ্রত্যাশিত ফলাফল হতে পারে
}
```

---

### ৪) API ডেটা ফেচ করার ক্ষেত্রে async/await এর গুরুত্ব কি?

**async/await** হলো জাভাস্ক্রিপ্টে অ্যাসিনক্রোনাস অপারেশন হ্যান্ডল করার একটি আধুনিক এবং সহজ পদ্ধতি।

**কেন async/await গুরুত্বপূর্ণ:**

**১. কোড পড়া সহজ হয়:**

```javascript
// Promise দিয়ে (পুরনো পদ্ধতি)
fetch('https://api.example.com/data')
    .then(response => response.json())
    .then(data => {
        console.log(data);
        return fetch('https://api.example.com/more');
    })
    .then(response => response.json())
    .then(moreData => console.log(moreData))
    .catch(error => console.error(error));

// async/await দিয়ে (আধুনিক পদ্ধতি)
async function getData() {
    try {
        const response = await fetch('https://api.example.com/data');
        const data = await response.json();
        console.log(data);
        
        const moreResponse = await fetch('https://api.example.com/more');
        const moreData = await moreResponse.json();
        console.log(moreData);
    } catch (error) {
        console.error(error);
    }
}
```

**২. এরর হ্যান্ডলিং সহজ:**
- `try/catch` ব্লক ব্যবহার করে সিনক্রোনাস কোডের মতোই এরর হ্যান্ডল করা যায়।

**৩. ডিবাগিং সহজ:**
- কোড লাইন বাই লাইন এক্সিকিউট হয় দেখায়, তাই ডিবাগ করা সহজ।

**৪. কন্ডিশনাল লজিক সহজ:**

```javascript
async function getUserData(userId) {
    const user = await fetch(`/api/users/${userId}`).then(r => r.json());
    
    if (user.isPremium) {
        const premiumData = await fetch(`/api/premium/${userId}`).then(r => r.json());
        return { ...user, ...premiumData };
    }
    
    return user;
}
```

**৫. লুপে ব্যবহার:**

```javascript
async function fetchAllUsers(ids) {
    const users = [];
    for (const id of ids) {
        const user = await fetch(`/api/users/${id}`).then(r => r.json());
        users.push(user);
    }
    return users;
}
```

---

### ৫) JavaScript এ Scope এর ধারণা ব্যাখ্যা করুন (Global, Function, Block)।

**Scope** হলো সেই এলাকা বা পরিসর যেখানে একটি ভেরিয়েবল অ্যাক্সেস করা যায়। জাভাস্ক্রিপ্টে তিন ধরনের স্কোপ আছে:

**১. Global Scope (গ্লোবাল স্কোপ):**

যখন কোনো ভেরিয়েবল সব ফাংশন এবং ব্লকের বাইরে ডিক্লেয়ার করা হয়, তখন সেটি গ্লোবাল স্কোপে থাকে। এই ভেরিয়েবল কোডের যেকোনো জায়গা থেকে অ্যাক্সেস করা যায়।

```javascript
// গ্লোবাল স্কোপ
var globalVar = "আমি গ্লোবাল";
let globalLet = "আমিও গ্লোবাল";
const globalConst = "আমিও গ্লোবাল";

function test() {
    console.log(globalVar);   // অ্যাক্সেস করা যায়
    console.log(globalLet);   // অ্যাক্সেস করা যায়
    console.log(globalConst); // অ্যাক্সেস করা যায়
}

test();
```

**২. Function Scope (ফাংশন স্কোপ):**

`var` দিয়ে ডিক্লেয়ার করা ভেরিয়েবল ফাংশন স্কোপড। মানে সেই ভেরিয়েবল শুধু ঐ ফাংশনের ভেতরেই অ্যাক্সেস করা যায়।

```javascript
function myFunction() {
    var functionVar = "আমি ফাংশনের ভেতরে";
    console.log(functionVar); // কাজ করবে
}

myFunction();
console.log(functionVar); // Error! ফাংশনের বাইরে অ্যাক্সেস করা যায় না
```

```javascript
function outer() {
    var outerVar = "বাইরের ফাংশন";
    
    function inner() {
        var innerVar = "ভেতরের ফাংশন";
        console.log(outerVar); // কাজ করবে (ক্লোজার)
    }
    
    inner();
    console.log(innerVar); // Error!
}
```

**৩. Block Scope (ব্লক স্কোপ):**

`let` এবং `const` দিয়ে ডিক্লেয়ার করা ভেরিয়েবল ব্লক স্কোপড। ব্লক হলো `{}` এর ভেতরের অংশ (if, for, while ইত্যাদি)।

```javascript
if (true) {
    var varVariable = "var দিয়ে";      // ফাংশন স্কোপড
    let letVariable = "let দিয়ে";      // ব্লক স্কোপড
    const constVariable = "const দিয়ে"; // ব্লক স্কোপড
}

console.log(varVariable);   // কাজ করবে (var ব্লক স্কোপড না)
console.log(letVariable);   // Error! ব্লকের বাইরে অ্যাক্সেস করা যায় না
console.log(constVariable); // Error! ব্লকের বাইরে অ্যাক্সেস করা যায় না
```

```javascript
for (var i = 0; i < 3; i++) {
    // var দিয়ে
}
console.log(i); // 3 (লুপের বাইরেও অ্যাক্সেস করা যায়)

for (let j = 0; j < 3; j++) {
    // let দিয়ে
}
console.log(j); // Error! (ব্লকের বাইরে অ্যাক্সেস করা যায় না)
```

 

## 🛠️ Technologies Used

- HTML5
- Tailwind CSS
- DaisyUI
- JavaScript (ES6+)
- Font Awesome Icons
- Fake Store API

## 📱 Screenshots

### Home Page
- Hero section with banner image
- Why Choose Us features
- Trending products

### Products Page
- Category filter buttons
- Product grid with cards
- Product details modal

## 👤 Author

**SwiftCart Team**
